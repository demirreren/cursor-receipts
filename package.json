{
  "name": "thermal-printer",
  "version": "1.0.0",
  "description": "USB thermal printer server",
  "main": "server.ts",
  "scripts": {
    "start": "bun server.ts",
    "dev": "bun --watch server.ts"
  },
  "dependencies": {
    "pngjs": "^7.0.0",
    "sharp": "^0.34.5",
    "usb": "^2.11.0"
  }
}
